map_server:
  ros__parameters:
    use_sim_time: False
    # yaml_filename is supplied by nav2_bringup.launch.py. Keeping a machine-
    # specific value here overrides the launch argument on ROS 2 Foxy.
