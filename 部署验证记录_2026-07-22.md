# Go2 自主导航项目部署验证记录

更新时间：2026-07-22  
设备：Jetson Orin NX / Ubuntu 20.04.6 / ARM64  
ROS：ROS 2 Foxy

## 当前结论

本机 ROS 2 Foxy 与项目基础运行环境已经部署完成。仓库内 8 个 ROS 2 包均已成功编译，离线 PCD 地图、二维栅格地图、定位节点、感知转换节点、Nav2 决策栈、Unitree 指令桥、WebSocket 桥和 TTS 节点已完成无硬件验证。

MID360、Livox 驱动和 FAST-LIO2 实时数据链路尚未部署，待雷达到货后进行。当前测试没有连接机器人，不包含真实运动、避障效果和导航闭环测试。

## 已部署组件

- ROS 2 Foxy Desktop、Nav2、rosbridge、pointcloud-to-laserscan
- colcon、rosdep、PCL、glog 等构建依赖
- Unitree 官方 `unitree_ros2`：`unitree_api`、`unitree_go`
- Python 依赖：websockets、edge-tts、transforms3d
- Go2 工作空间：`/home/nvidia/Go2_Nav_ws`
- Unitree 工作空间：`/home/nvidia/unitree_ros2/cyclonedds_ws`

新终端会自动加载 Foxy、Unitree 消息包和 Go2 工作空间环境。

## 构建结果

以下 8 个包全部构建成功：

1. `Go2_web_bridge`
2. `fast_lio_localization_ros2`
3. `go2_bringup`
4. `go2_nav2`
5. `go2_pc2scan`
6. `go2_time_sync`
7. `odom_tf_bridge`
8. `pcd_to_map`

`colcon test` 可正常运行，但仓库当前没有定义自动化测试用例，结果为 `0 tests, 0 errors, 0 failures`。

## 已完成的运行验证

### ROS 2 基础环境

- `ROS_DISTRO=foxy`
- `ros2` CLI 正常
- demo talker/listener DDS 通信通过
- Nav2、rosbridge 等依赖包版本检查通过

### 离线地图

- 仓库 PCD：118,496 点
- PCD 转二维地图功能运行成功
- 测试输出：781 × 877、0.05 m/cell
- 仓库二维地图：783 × 878、0.05 m/cell
- Nav2 map_server 成功加载地图并进入 `active` 状态
- RViz2 成功加载 `/map3d` 离线点云地图

### 定位与感知

- `odom_tf_bridge` 可启动，接口为 `/Odometry` → `/odom` + `odom→base_link`
- `pcd_publisher` 成功加载并发布 PCD 地图
- `global_localization` 成功接收并降采样全局地图，当前等待 MID360 扫描
- `transform_fusion` 可启动
- 点云过滤、pointcloud-to-laserscan、QoS relay 三节点均可启动

### Nav2

使用静态 TF 和模拟 `/scan` 完成无硬件启动测试：

- map_server：active
- planner_server：active
- controller_server：active
- recoveries_server：active
- bt_navigator：已激活
- `/navigate_to_pose`、`/compute_path_to_pose`、`/follow_path`、`/spin`、`/backup`、`/wait` Action 均已注册
- `/map`、全局/局部 costmap、`/plan`、`/cmd_vel` 等话题均已创建
- 启动日志无 ERROR/FATAL

### Unitree 与 Web

- `go2_cmd_vel_bridge_node` 编译并可启动
- `/api/sport/request` 的 Unitree 消息发布端已创建
- WebSocket 节点可启动，服务器不可达时按配置自动重连
- TTS 节点可启动；实际扬声器播放待 USB 音频设备接入后验证

## 本轮修复

1. 将 `maps/MID360_map.yaml` 的图片路径由旧机器绝对路径改为相对路径。
2. `go2_autostart.sh` 默认地图路径改为根据当前工作空间自动推导。
3. 修复 `pcd_to_map` 在 Foxy 下 launch 参数被 YAML 默认值覆盖的问题。
4. 修复 `pcd_to_map` 完成或加载失败时在 shutdown 后创建执行器导致的崩溃。
5. 移除 Nav2 map_server 配置中的旧机器绝对路径，地图改由 launch 参数提供。
6. 修复 `run_nav2.sh` 对 Unitree 官方工作空间安装路径的识别。
7. 修复 ROS 2 Foxy 安装脚本验证前未加载 ROS 环境的问题。

## 待硬件到货后完成

1. 安装并配置 `livox_ros_driver2`。
2. 安装 FAST-LIO2，核对 MID360 的 13° 安装外参。
3. 配置雷达网卡、IP、MTU、时间源和点云 QoS。
4. 实测 `/livox/lidar`、`/Odometry`、`/cloud_registered`、`/cloud_registered_body` 频率。
5. 验证 ICP 首次定位、失锁恢复和长时间漂移修正。
6. 验证真实 `/scan`、局部代价地图和动态障碍物效果。
7. 根据实际机器人网卡配置 CycloneDDS 接口，并安装系统级 CycloneDDS RMW 包。
8. 低速测试 `/cmd_vel` → Go2 Sport API，确认急停、超时停车和速度上限。
9. 完成真实路径规划、避障、恢复行为和长时间巡航测试。
10. 接入 USB 音频设备并验证中文 TTS。

## 已知事项

- ROS 2 Foxy 已停止官方维护，当前部署以 Ubuntu 20.04 官方归档包为基础。
- `Go2_web_bridge` 包名包含大写字母，不符合 ROS 包命名规范，但不影响当前构建运行。
- PCL 构建期间存在 VTK 可选组件警告，不影响当前 PCD 读取、ICP 和地图转换。
- 飞书页面在当前执行环境中无法访问，本文可作为线上文档的更新内容。
